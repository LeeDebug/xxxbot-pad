## 启动命令

```bash
# 启动
$ docker-compose -f docker-compose.yml up -d
$ docker-compose -f docker-compose.yml down
# 启动(重命名)
$ docker-compose -p xxxbot-pad -f docker-compose.yml up -d
$ docker-compose -p xxxbot-pad -f docker-compose.yml down
```
